{
  "_readme": "Codes required to CREATE a room (joining an existing room needs no code). Case-insensitive, surrounding spaces ignored. To disable a code set enabled to false; to add one, append a new entry. The server reads this file at startup, so changes take effect on the next deploy/restart.",
  "codes": [
    { "code": "rougemont", "enabled": true },
    { "code": "netflix", "enabled": true },
    { "code": "apple", "enabled": true },
    { "code": "peach", "enabled": true }
  ]
}
